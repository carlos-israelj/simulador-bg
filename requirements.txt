# Dependencias del microservicio simulador BG (versiones verificadas)
fastapi==0.138.0
uvicorn==0.49.0
playwright==1.60.0
pydantic==2.13.4
stem==1.8.2
openpyxl==3.1.2

# Notas:
# - Tras instalar, hay que bajar el navegador:  playwright install chromium
# - stem es necesario para rotación de IPs con TOR (control de TOR)
# - openpyxl es necesario para generar archivos Excel
# - starlette / anyio / pydantic-core se instalan solos como dependencias.
